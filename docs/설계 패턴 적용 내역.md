# 8-1 적용 패턴 개요
| 항목 | 내용 |
|----|--------------|
|패턴명|싱글톤 패턴|
|분류|생성|
|적용 대상 클래스|FilterManager, ExportService|
|선택이유|야구 정보 조회 시스템에서는 대용량의 선수 원천 데이터 세트와 세이버메트릭스 지표 연산용 필터를 전역에서 일관되게 관리해야 합니다. FilterManager 인스턴스가 여러 개 생성될 경우 필터 기준이 혼선되거나 리소스 낭비가 발생할 수 있습니다. 또한 기기 저장용 파일 출력을 담당하는 ExportService 역시 시스템 내에서 단 하나의 제어권을 유지해야 파일 쓰기 충돌이나 핸들러 낭비가 없습니다.두 클래스의 생성자를 private으로 제한하고 내부에서 단 하나의 정적 객체 인스턴스만을 유지하도록 getInstance() 메서드를 구현하여 시스템 전역에서 동일한 필터 상태와 출력 엔진을 공유하도록 설계했습니다.|

| 항목 | 내용 |
|----|--------------|
|패턴명|전략 패턴|
|분류|행동|
|적용 대상 클래스|PlayerService, DashboardRenderStrategy|
|선택이유|기기 환경이나 사용자 등급(입문자 모드, 일반 팬 모드, 분석가 모드)에 따라 화면에 데이터를 바인딩하고 시각화하는 알고리즘이 완전히 달라집니다. 이를 단일 메서드 내의 거대한 if-else문으로 처리하면 향후 새로운 지표나 시각화 모드가 추가될 때 소스 코드가 매우 복잡해지고 유지보수가 불가능해집니다.대시보드 렌더링 방식을 DashboardRenderStrategy 인터페이스로 정의하여 각 모드별 알고리즘 클래스(BeginnerRender, FanRender, ExpertRender)로 독립 분리했습니다. PlayerService는 사용자가 선택한 모드 전략 객체를 주입받아 동적으로 renderComparisonView()를 수행하도록 OCP(개방-폐쇄 원칙)를 준수하여 설계했습니다.|

# 8-2 패턴 적용 다이어그램

#변경전
```mermaid
graph LR
    %% Actors
    User["일반 사용자 (User)"]
    Admin["관리자 (Admin)"]

    subgraph "선수 데이터 시스템"
        %% Use Cases
        UC1("(FR-01) 선수 데이터 내보내기")
        UC1_sub("주요 지표 이미지로 저장하기")
        
        UC2("선수 정보 비교하기")
        UC2_sub1("(FR-02) 다중 항목 정의하기")
        UC2_sub2("(FR-03) 레이더 차트 화면 구현하기")
        
        UC3("(FR-04) 세이버메트릭스 구조 활용하기")
        UC3_sub("(FR-05) 유용한 필터만 남기기")
    end

    %% Relationships: Actor to UC
    User --> UC1
    User --> UC2
    User --> UC3
    Admin --> UC3_sub

    %% Include Relationships
    UC1 -.->|"<<include>>"| UC1_sub
    UC2 -.->|"<<include>>"| UC2_sub1
    UC2 -.->|"<<include>>"| UC2_sub2

    %% Extend Relationships
    UC3_sub -.->|"<<extend>>"| UC3

    %% Styling
    style User fill:#fff3e0,stroke:#ff9800,stroke-width:2px
    style Admin fill:#fff3e0,stroke:#ff9800,stroke-width:2px
    style UC1 fill:#e8f5e9,stroke:#2e7d32
    style UC2 fill:#e8f5e9,stroke:#2e7d32
    style UC3 fill:#e8f5e9,stroke:#2e7d32
    style UC1_sub fill:#e1f5fe,stroke:#01579b
    style UC2_sub1 fill:#e1f5fe,stroke:#01579b
    style UC2_sub2 fill:#e1f5fe,stroke:#01579b
    style UC3_sub fill:#ffebee,stroke:#c62828
```


#변경후
```mermaid
classDiagram
    %% 클래스 정의
    class User {
        +String userId
        +String userName
        +login() void
        +logout() void
    }

    class Admin {
        +int adminLevel
        +manageSystem() void
        +optimizeFilterSettings() void
    }

    %% 전략 패턴 적용: 대시보드 렌더링 분리
    class PlayerService {
        -DashboardRenderStrategy renderStrategy
        +List selectedAttributes
        +String displayType
        +setRenderStrategy(strategy) void
        +selectComparisonItems(items) void
        +renderComparisonView(playerIds) void
    }

    class DashboardRenderStrategy {
        <<interface>>
        +renderDashboard(playerIds) void
    }

    class BeginnerRender {
        +showSimpleStats() void
        +renderDashboard(playerIds) void
    }

    class FanRender {
        +showStandardCharts() void
        +renderDashboard(playerIds) void
    }

    class ExpertRender {
        +showSabermetricsDetails() void
        +renderDashboard(playerIds) void
    }

    %% 싱글톤 패턴 적용 1: 전역 필터 관리
    class FilterManager {
        -static FilterManager instance
        +Map dataStructure
        +List activeFilters
        -FilterManager()
        +static getInstance() FilterManager
        +applyDataStructure(context) void
        +removeUnusedFilters() void
    }

    %% 싱글톤 패턴 적용 2: 전역 단일 출력 엔진
    class ExportService {
        -static ExportService instance
        +Object captureTarget
        +String exportFormat
        -ExportService()
        +static getInstance() ExportService
        +captureUI() void
        +saveAsImage() void
    }

    class PlayerData {
        +String name
        +double battingAverage
        +String teamName
        +getStats() Map
    }

    %% 관계 설정
    User <|-- Admin : 상속 (Inheritance)
    User ..> PlayerService : 사용 (Dependency)
    User ..> ExportService : 사용 (Dependency)
    Admin ..> FilterManager : 관리 (Dependency)
    
    PlayerService --> PlayerData : 참조 (Association)
    PlayerService ..> ExportService : 이미지 저장 요청 (Dependency)
    FilterManager o-- PlayerData : 데이터 필터링 (Aggregation)
    
    %% 전략 패턴 관계 매핑
    PlayerService --> DashboardRenderStrategy : 전략 위임 (Aggregation)
    DashboardRenderStrategy <|.. BeginnerRender : 실체화 (Realization)
    DashboardRenderStrategy <|.. FanRender : 실체화 (Realization)
    DashboardRenderStrategy <|.. ExpertRender : 실체화 (Realization)
```
